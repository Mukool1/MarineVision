import { useEffect, useState } from 'react'
import { getUsers } from '../api/api'
export default function AdminPage() {
  const [users, setUsers] = useState([]); const [error, setError] = useState('')
  useEffect(() => { getUsers().then(setUsers).catch(e => setError(e?.response?.data?.detail || 'Could not load users')) }, [])
  return <div className="mx-auto max-w-[1080px]"><h1 className="m-0 text-2xl font-bold tracking-tight text-[#073654]">Administration</h1><p className="mb-5 mt-1 text-xs text-slate-500">Manage access and review workspace activity.</p><section className="panel overflow-hidden p-0"><div className="border-b border-line p-5"><p className="eyebrow">Users</p><h2 className="m-0 text-lg font-bold text-[#18383a]">Registered operators</h2></div>{error ? <p className="p-5 text-sm text-rose-600">{error}</p> : <div className="overflow-x-auto"><table className="w-full min-w-[600px] text-left text-sm"><thead className="bg-[#f3f8f6] text-[10px] uppercase tracking-wider text-muted"><tr><th className="px-5 py-3">Name</th><th className="px-3 py-3">Email</th><th className="px-3 py-3">Role</th><th className="px-5 py-3">Scans</th></tr></thead><tbody>{users.map(user => <tr key={user.id} className="border-t border-line"><td className="px-5 py-3 font-medium">{user.full_name}</td><td className="px-3 py-3 text-muted">{user.email}</td><td className="px-3 py-3"><span className="rounded-full bg-sky-50 px-2 py-1 text-xs font-bold text-sky-700">{user.role}</span></td><td className="px-5 py-3">{user.scan_count}</td></tr>)}</tbody></table></div>}</section></div>
}
